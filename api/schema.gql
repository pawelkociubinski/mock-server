type Task {
  id: Int!
  parentId: Int
  slug: String!
  statusId: Int!
  tagsId: [Int!]!
  title: String!
  userId: Int!
}

type User {
  fullName: String!
  id: Int!
}

type Status {
  id: Int!
  name: String!
  slug: String!
}

type Tag {
  id: Int!
  name: String!
  slug: String!
}

type Query {
  statuses: [Status!]!
  tags: [Tag!]!
  tasks: [Task!]!
  users: [User!]!
}

type Mutation {
  addTagForTask(taskId: Int!, tagName: String!): Task!
  createTask(parentId: Int, title: String!, userId: Int!): Task!
  removeTagFromTask(taskId: Int!, tagId: Int!): Task!
}
